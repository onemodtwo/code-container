diff --git a/code-container-orig/src/args.ts b/code-container-orig/src/args.ts
new file mode 100644
index 0000000..54e1a07
--- /dev/null
+++ b/code-container-orig/src/args.ts
@@ -0,0 +1,146 @@
+import { printError } from "./utils";
+import { BuildTarget } from "./docker";
+
+const BUILD_TARGETS: BuildTarget[] = ["full", "packages", "harness", "user"];
+
+export type ParsedArgs =
+  | { command: "run"; projectPath: string; cliFlags: string[] }
+  | { command: "build"; target: BuildTarget }
+  | { command: "init" }
+  | { command: "stop"; projectPath: string }
+  | { command: "remove"; projectPath: string }
+  | { command: "list" }
+  | { command: "clean" };
+
+function usage(): never {
+  console.log(`
+Usage: container [COMMAND] [PROJECT_PATH] [-- DOCKER_FLAGS...]
+
+Manage Code containers for isolated project environments.
+
+Commands:
+    (none)         Start container for current directory (default)
+    run            Start container for specified project path
+    build [TARGET] Build the Docker image (default: full)
+    init           Copy config files from home directory
+    stop           Stop the container for this project
+    remove         Remove the container for this project
+    list           List all Code containers
+    clean          Remove all stopped Code containers
+
+Build targets:
+    full           Build all stages from scratch (Core -> Packages -> Harness -> User)
+    packages       Rebuild from Packages stage onward
+    harness        Rebuild from Harness stage onward
+    user           Rebuild User stage only
+
+Arguments:
+    PROJECT_PATH    Path to the project directory (defaults to current directory)
+    DOCKER_FLAGS    Additional flags passed to 'docker run' after '--'
+
+Examples:
+    container                           # Start container for current directory
+    container run /path/to/project      # Start container for specific project
+    container run /path -- -p 8080:80   # Pass Docker flags for port mapping
+    container run -- -e FOO=bar         # Pass env vars (uses current directory)
+    container build                     # Build all stages from scratch
+    container build full                # Build all stages from scratch
+    container build packages            # Rebuild from Packages stage
+    container build harness             # Rebuild from Harness stage
+    container build user                # Rebuild User stage only
+    container init                      # Copy config files
+    container stop                      # Stop container for current directory
+    container remove /path/to/project   # Remove container for specific project
+    container list                      # List all containers
+    container clean                     # Clean up stopped containers
+`);
+  process.exit(0);
+}
+
+function fatal(msg: string[]): never {
+  msg.forEach(printError);
+  process.exit(1);
+}
+
+const VALID_COMMANDS = [
+  "run",
+  "build",
+  "init",
+  "stop",
+  "remove",
+  "list",
+  "clean",
+];
+
+function splitAtSeparator(args: string[]): {
+  before: string[];
+  after: string[];
+} {
+  const idx = args.indexOf("--");
+  if (idx === -1) {
+    return { before: args, after: [] };
+  }
+  return { before: args.slice(0, idx), after: args.slice(idx + 1) };
+}
+
+export function parseArgs(raw: string[]): ParsedArgs {
+  if (raw.length === 0) {
+    return { command: "run", projectPath: "", cliFlags: [] };
+  }
+
+  const first = raw[0];
+  if (first === "help" || first === "--help" || first === "-h") {
+    usage();
+  }
+
+  if (!VALID_COMMANDS.includes(first)) {
+    fatal([`Unknown command: ${first}`]);
+  }
+
+  const command = first as (typeof VALID_COMMANDS)[number];
+  const remaining = raw.slice(1);
+
+  switch (command) {
+    case "build": {
+      const target = remaining[0] || "full";
+      if (!BUILD_TARGETS.includes(target as BuildTarget)) {
+        fatal([
+          `Unknown build target: ${target}`,
+          `Available targets: ${BUILD_TARGETS.join(", ")}`,
+        ]);
+      }
+      return { command: "build", target: target as BuildTarget };
+    }
+    case "list":
+    case "clean":
+    case "init": {
+      if (remaining.length > 0) {
+        fatal([`Unexpected argument: ${remaining[0]}`]);
+      }
+      return { command } as ParsedArgs;
+    }
+    case "run": {
+      const { before, after } = splitAtSeparator(remaining);
+      if (before.length > 1) {
+        fatal([`Unexpected argument: ${before[1]}`]);
+      }
+      return {
+        command: "run",
+        projectPath: before[0] || "",
+        cliFlags: after,
+      };
+    }
+    case "stop":
+    case "remove": {
+      if (remaining.length > 1) {
+        fatal([`Unexpected argument: ${remaining[1]}`]);
+      }
+      return {
+        command: command as "stop" | "remove",
+        projectPath: remaining[0] || "",
+      };
+    }
+  }
+
+  return fatal(["Unreachable"]);
+}
diff --git a/code-container/src/commands.ts b/code-container-orig/src/commands.ts
index 9d31763..68fecd7 100644
--- a/code-container/src/commands.ts
+++ b/code-container-orig/src/commands.ts
@@ -1,11 +1,11 @@
-import * as path from "path";
-import * as fs from "fs";
-import * as os from "os";
+import path from "path";
+import fs from "fs";
 import {
   printInfo,
   printSuccess,
   printWarning,
   printError,
+  promptYesNo,
 } from "./utils";
 import {
   generateContainerName,
@@ -19,172 +19,122 @@ import {
   createNewContainer,
   execInteractive,
   stopContainerIfLastSession,
-  trackSessionStart,
-  trackSessionEnd,
   listContainersRaw,
   getStoppedContainerIds,
   removeContainersById,
   IMAGE_NAME,
   IMAGE_TAG,
+  BuildTarget,
 } from "./docker";
-import { ensureProjectDir, copyClaudeConfigs, generateProjectDirName, PROJECTS_DIR, APPDATA_DIR } from "./config";
-import { buildMounts } from "./mounts";
-import { loadMountConfig, loadGlobalConfig } from "./mount-config";
-
-export function buildImage(): void {
-  const { base_image, timezone, claude_version } = loadGlobalConfig();
-  printInfo(`Building container image: ${IMAGE_NAME}:${IMAGE_TAG}`);
-  printInfo(`Base image: ${base_image}`);
-  printInfo(`Timezone: ${timezone}`);
-  printInfo(`Claude version: ${claude_version}`);
-  if (!buildImageRaw(base_image, timezone, claude_version)) {
-    printError("Failed to build container image");
-    process.exit(1);
-  }
-  printSuccess("Container image built successfully");
-}
-
-/**
- * Copies the host's Claude credentials and settings into the project's
- * config directory. The project directory is created if it doesn't exist.
- * Run this on first setup or after re-authenticating on the host.
- */
-function rejectAppdataPath(projectPath: string): void {
-  if (projectPath === APPDATA_DIR || projectPath.startsWith(APPDATA_DIR + path.sep)) {
-    printError(`Cannot use a path inside ~/.code-container/ as a project directory.`);
+import {
+  ensureConfigDir,
+  loadSettings,
+  saveSettings,
+  copyConfigs,
+} from "./config";
+
+export function buildImage(target: BuildTarget): void {
+  printInfo(`Building Docker image: ${IMAGE_NAME}:${IMAGE_TAG}`);
+  if (!buildImageRaw(target)) {
+    printError("Failed to build Docker image");
     process.exit(1);
   }
+  printSuccess("Docker image built successfully");
 }
 
-export function init(projectPath: string): void {
-  rejectAppdataPath(projectPath);
-  if (!fs.existsSync(projectPath) || !fs.statSync(projectPath).isDirectory()) {
-    printError(`Project directory does not exist: ${projectPath}`);
-    process.exit(1);
+export async function init(isStartup: boolean = false): Promise<void> {
+  const settings = loadSettings();
+
+  if (isStartup) {
+    // If startup: Check if completedInit, if not, prompt.
+    if (!settings.completedInit) {
+      printInfo("First run detected. Would you like to copy config files?");
+      printInfo(
+        "This will copy your OpenCode, Codex, Claude Code, & Gemini CLI configs to ~/.code-container/configs for mounting.",
+      );
+      printInfo(
+        "If you choose to not copy config files, you can still setup your harness once inside the container.",
+      );
+
+      const shouldCopy = await promptYesNo("Copy config files?");
+      if (shouldCopy) {
+        copyConfigs();
+        printSuccess("Config files copied successfully");
+      } else {
+        printInfo("Ignoring copy. Run `container init` to copy.");
+      }
+    }
+    settings.completedInit = true;
+    saveSettings(settings);
+  } else {
+    // Else, not startup; user ran container init
+    if (!settings.completedInit) {
+      printInfo("Copying config files to ~/.code-container/configs...");
+      copyConfigs();
+      printSuccess("Config files copied successfully");
+
+      settings.completedInit = true;
+      saveSettings(settings);
+    } else {
+      printWarning(
+        "Config files already exist. This operation will merge and overwrite existing config files.",
+      );
+      const shouldCopy = await promptYesNo("Continue?");
+      if (shouldCopy) {
+        copyConfigs();
+        printSuccess("Config files copied successfully");
+      }
+    }
   }
-
-  const dirName = generateProjectDirName(projectPath);
-  const mountConfig = loadMountConfig(dirName);
-  ensureProjectDir(dirName, mountConfig.claude_auth_mode);
-  copyClaudeConfigs(dirName, mountConfig.claude_auth_mode);
-
-  printSuccess("Claude credentials copied to project directory.");
-  printInfo(
-    "Note: container permissions are set by ~/.code-container/projects/<name>/settings.json " +
-    "not your host ~/.claude/settings.json. Edit that file directly to change container permissions."
-  );
 }
 
-export async function runContainer(projectPath: string): Promise<void> {
-  rejectAppdataPath(projectPath);
+export async function runContainer(
+  projectPath: string,
+  cliFlags: string[] = [],
+): Promise<void> {
   const containerName = generateContainerName(projectPath);
-  const projectDirName = generateProjectDirName(projectPath);
+  const projectName = path.basename(projectPath);
 
   if (!fs.existsSync(projectPath) || !fs.statSync(projectPath).isDirectory()) {
     printError(
-      `Project directory does not exist or is not a directory: ${projectPath}`
+      `Project directory does not exist or is not a directory: ${projectPath}`,
     );
     process.exit(1);
   }
 
-  // Load mount config early so claude_auth_mode is known before ensureProjectDir.
-  const mountConfig = loadMountConfig(projectDirName);
-
-  // Ensure project config directory and Claude stub files exist.
-  // Pass the effective auth mode so per-project overrides are respected.
-  ensureProjectDir(projectDirName, mountConfig.claude_auth_mode);
-
-  // In per_project auth mode, ensure .claude.json exists in the project dir.
-  // This handles the case where the mode was later switched from shared to
-  // per_project via override.json after the project dir was already created.
-  if (mountConfig.claude_auth_mode === "per_project") {
-    const projectClaudeJson = path.join(PROJECTS_DIR, projectDirName, ".claude.json");
-    if (!fs.existsSync(projectClaudeJson)) {
-      copyClaudeConfigs(projectDirName, mountConfig.claude_auth_mode);
-    }
-  }
+  ensureConfigDir();
 
   if (!imageExists()) {
-    printWarning("Container image not found. Building...");
-    buildImage();
-  }
-
-  const { penv_path, renv_path, extra_ld_library_path, forward_ssh_agent } = mountConfig;
-  const relayDir = path.join(os.homedir(), ".ssh-agent-relay");
-  const relaySock = path.join(relayDir, "agent.sock");
-  const sshAgentPath = forward_ssh_agent ? relaySock : "";
-  const sessionDir = path.join(PROJECTS_DIR, projectDirName, "sessions");
-
-  // Warn if forwarding is enabled but the relay socket does not exist yet.
-  if (forward_ssh_agent && !fs.existsSync(relaySock)) {
-    printWarning(`SSH agent forwarding enabled but relay socket not found at ${relaySock} — SSH agent will not be available until the relay is running.`);
-  }
-
-  // Warn if known_hosts is configured but the file has moved or been deleted.
-  const { ssh_known_hosts_path } = mountConfig;
-  if (forward_ssh_agent && ssh_known_hosts_path && !fs.existsSync(ssh_known_hosts_path)) {
-    printWarning(`SSH known_hosts file not found at ${ssh_known_hosts_path} — host key verification will fail inside the container. Update ssh_known_hosts_path in ~/.code-container/config.json (or the project override.json).`);
+    printWarning("Docker image not found. Building...");
+    buildImage("full");
   }
 
   if (containerRunning(containerName)) {
     printInfo(`Container '${containerName}' is already running`);
     printInfo("Attaching to container...");
-    const sessionFile = trackSessionStart(sessionDir);
-    try {
-      execInteractive(containerName, projectPath, penv_path, renv_path, extra_ld_library_path, sshAgentPath);
-    } finally {
-      trackSessionEnd(sessionFile);
-    }
-    if (!mountConfig.keep_alive) stopContainerIfLastSession(containerName, sessionDir);
+    execInteractive(containerName, projectName);
+    stopContainerIfLastSession(containerName, projectName);
     return;
   }
 
   if (containerExists(containerName)) {
     printInfo(`Starting existing container: ${containerName}`);
-    printWarning(
-      "Note: existing containers retain their original mount and network configuration. " +
-      "Run `container remove` then `container run` to apply config changes."
-    );
     startContainer(containerName);
-    const sessionFile = trackSessionStart(sessionDir);
-    try {
-      execInteractive(containerName, projectPath, penv_path, renv_path, extra_ld_library_path, sshAgentPath);
-    } finally {
-      trackSessionEnd(sessionFile);
-    }
-    if (!mountConfig.keep_alive) stopContainerIfLastSession(containerName, sessionDir);
+    execInteractive(containerName, projectName);
+    stopContainerIfLastSession(containerName, projectName);
     return;
   }
 
-  // Ensure the relay directory exists before buildMounts tries to mount it —
-  // the relay may not have run yet if no SSH session has been established.
-  if (forward_ssh_agent) {
-    try {
-      fs.mkdirSync(relayDir, { recursive: true, mode: 0o700 });
-    } catch (e: any) {
-      if (e?.code !== "EEXIST") printWarning(`Could not create SSH agent relay directory: ${e?.message}`);
-    }
-  }
-
-  const mounts = buildMounts(projectPath, projectDirName, mountConfig);
-
   printInfo(`Creating new container: ${containerName}`);
   printInfo(`Project: ${projectPath}`);
-  printInfo(`Network: ${mountConfig.network}`);
-  printInfo(`Keep alive: ${mountConfig.keep_alive}`);
 
-  if (!createNewContainer(containerName, projectPath, mounts, mountConfig.network, penv_path, renv_path, extra_ld_library_path, sshAgentPath)) {
+  if (!createNewContainer(containerName, projectName, projectPath, cliFlags)) {
     printError("Failed to create container");
     process.exit(1);
   }
 
-  const sessionFile = trackSessionStart(sessionDir);
-  try {
-    execInteractive(containerName, projectPath, penv_path, renv_path, extra_ld_library_path, sshAgentPath);
-  } finally {
-    trackSessionEnd(sessionFile);
-  }
-  if (!mountConfig.keep_alive) stopContainerIfLastSession(containerName, sessionDir);
+  execInteractive(containerName, projectName);
+  stopContainerIfLastSession(containerName, projectName);
   printSuccess("Container session ended");
 }
 
diff --git a/code-container/src/config.ts b/code-container-orig/src/config.ts
index 6768295..5aa6779 100644
--- a/code-container/src/config.ts
+++ b/code-container-orig/src/config.ts
@@ -1,69 +1,37 @@
-import * as path from "path";
-import * as fs from "fs";
-import * as os from "os";
-import * as crypto from "crypto";
+import path from "path";
+import fs from "fs";
+import os from "os";
 import { z } from "zod";
 
 export const APPDATA_DIR = path.join(os.homedir(), ".code-container");
-
-const HOST_CONFIG_PATH = path.join(APPDATA_DIR, "config.json");
-
-// Read a field from config.json. Returns undefined if absent or unreadable.
-function readConfigField<T>(field: string): T | undefined {
-  try {
-    if (fs.existsSync(HOST_CONFIG_PATH)) {
-      const cfg = JSON.parse(fs.readFileSync(HOST_CONFIG_PATH, "utf-8"));
-      return cfg[field] as T;
-    }
-  } catch { /* ignore */ }
-  return undefined;
-}
-
-function loadClaudePermissions(): { allow: string[]; deny: string[] } {
-  return readConfigField<{ allow: string[]; deny: string[] }>("claude_permissions")
-    ?? { allow: ["*"], deny: [] };
-}
-
-function loadAuthMode(): "shared" | "per_project" {
-  return readConfigField<"shared" | "per_project">("claude_auth_mode") ?? "shared";
-}
-export const PROJECTS_DIR = path.join(APPDATA_DIR, "projects");
-export const DOCKERFILE_PATH = path.join(APPDATA_DIR, "Dockerfile");
-export const TOS_SETTINGS_PATH = path.join(APPDATA_DIR, "settings.json");
+export const CONFIGS_DIR = path.join(APPDATA_DIR, "configs");
+export const USER_DOCKERFILE_PATH = path.join(APPDATA_DIR, "Dockerfile.User");
+export const PACKAGES_DOCKERFILE_PATH = path.join(
+  APPDATA_DIR,
+  "Dockerfile.Packages",
+);
+export const SETTINGS_PATH = path.join(APPDATA_DIR, "settings.json");
+export const MOUNTS_PATH = path.join(APPDATA_DIR, "MOUNTS.txt");
+export const FLAGS_PATH = path.join(APPDATA_DIR, "DOCKER_FLAGS.txt");
+export const RUN_FLAGS_PATH = path.join(APPDATA_DIR, "DOCKER_RUN_FLAGS.txt");
+
+export const SHARED_DIRS = [
+  ".claude",
+  ".codex",
+  ".copilot",
+  ".local/share",
+  ".local/state",
+  ".opencode",
+  ".gemini",
+];
 
 const SettingsSchema = z.object({
+  completedInit: z.boolean().default(false),
   acceptedTos: z.boolean().default(false),
 });
 
 export type Settings = z.infer<typeof SettingsSchema>;
 
-// Canonicalize a project path so that symlinks and trailing slashes don't
-// produce different hashes for the same physical directory.
-// Falls back to path.resolve() if the path doesn't exist yet.
-function canonicalizeProjectPath(projectPath: string): string {
-  const stripped = projectPath.replace(/\/$/, "");
-  try {
-    return fs.realpathSync(stripped);
-  } catch {
-    return path.resolve(stripped);
-  }
-}
-
-export function generateProjectHash(projectPath: string): string {
-  return crypto
-    .createHash("sha1")
-    .update(canonicalizeProjectPath(projectPath))
-    .digest("hex")
-    .substring(0, 8);
-}
-
-export function generateProjectDirName(projectPath: string): string {
-  const canonicalPath = canonicalizeProjectPath(projectPath);
-  const projectName = path.basename(canonicalPath);
-  const hash = generateProjectHash(projectPath);
-  return `${projectName}-${hash}`;
-}
-
 export function ensureAppdataDir(): void {
   if (!fs.existsSync(APPDATA_DIR)) {
     fs.mkdirSync(APPDATA_DIR, { recursive: true, mode: 0o700 });
@@ -73,103 +41,70 @@ export function ensureAppdataDir(): void {
 }
 
 export function loadSettings(): Settings {
-  if (!fs.existsSync(TOS_SETTINGS_PATH)) {
-    return { acceptedTos: false };
-  }
-  try {
-    const content = fs.readFileSync(TOS_SETTINGS_PATH, "utf-8");
-    return SettingsSchema.parse(JSON.parse(content));
-  } catch {
-    return { acceptedTos: false };
+  if (!fs.existsSync(SETTINGS_PATH)) {
+    return { completedInit: false, acceptedTos: false };
   }
+  const content = fs.readFileSync(SETTINGS_PATH, "utf-8");
+  return SettingsSchema.parse(JSON.parse(content));
 }
 
 export function saveSettings(settings: Settings): void {
   ensureAppdataDir();
-  fs.writeFileSync(TOS_SETTINGS_PATH, JSON.stringify(settings, null, 2), {
+  fs.writeFileSync(SETTINGS_PATH, JSON.stringify(settings, null, 2), {
     mode: 0o600,
   });
 }
 
-// authMode parameter lets callers pass the effective auth mode from MountConfig
-// (which includes per-project override.json). Falls back to reading the global
-// config.json directly when no override context is available (e.g. `container init`).
-export function ensureProjectDir(
-  projectDirName: string,
-  authMode?: "shared" | "per_project"
-): string {
-  ensureAppdataDir();
-  const projectDir = path.join(PROJECTS_DIR, projectDirName);
-  const effectiveAuthMode = authMode ?? loadAuthMode();
-
-  if (!fs.existsSync(projectDir)) {
-    fs.mkdirSync(projectDir, { recursive: true, mode: 0o700 });
-    // Only copy .claude.json in per_project auth mode — in shared mode the host
-    // file is mounted directly and no project-local copy is needed.
-    if (effectiveAuthMode === "per_project") {
-      _copyClaudeConfigsIfPresent(projectDir);
+const CONFIG_SOURCES: Array<{ src: string; dest: string; isDir: boolean }> = [
+  {
+    src: path.join(os.homedir(), ".config", "opencode"),
+    dest: ".opencode",
+    isDir: true,
+  },
+  { src: path.join(os.homedir(), ".codex"), dest: ".codex", isDir: true },
+  { src: path.join(os.homedir(), ".copilot"), dest: ".copilot", isDir: true },
+  { src: path.join(os.homedir(), ".gemini"), dest: ".gemini", isDir: true },
+  { src: path.join(os.homedir(), ".claude"), dest: ".claude", isDir: true },
+  {
+    src: path.join(os.homedir(), ".claude.json"),
+    dest: ".claude.json",
+    isDir: false,
+  },
+];
+
+export function copyConfigs(): void {
+  ensureConfigDir();
+
+  for (const { src, dest, isDir } of CONFIG_SOURCES) {
+    const destPath = path.join(CONFIGS_DIR, dest);
+    if (fs.existsSync(src)) {
+      if (isDir) {
+        fs.cpSync(src, destPath, { recursive: true });
+      } else {
+        fs.copyFileSync(src, destPath);
+      }
     }
   }
+}
 
-  // Ensure required subdirectories exist
-  for (const subdir of ["claude-projects", "claude-statsig"]) {
-    const subdirPath = path.join(projectDir, subdir);
-    if (!fs.existsSync(subdirPath)) {
-      fs.mkdirSync(subdirPath, { recursive: true, mode: 0o700 });
-    }
-  }
+export function ensureConfigDir(): void {
+  ensureAppdataDir();
 
-  // Ensure settings.json exists (read-only inside container — agent cannot self-modify permissions).
-  // Permissions come from claude_permissions in ~/.code-container/config.json,
-  // which is seeded from install.json at install time. Edit config.json to change
-  // the default for new projects; edit the project's settings.json directly to
-  // change an existing project.
-  const settingsPath = path.join(projectDir, "settings.json");
-  if (!fs.existsSync(settingsPath)) {
-    const claudeSettings = { permissions: loadClaudePermissions() };
-    fs.writeFileSync(settingsPath, JSON.stringify(claudeSettings, null, 2) + "\n", { mode: 0o600 });
+  if (!fs.existsSync(CONFIGS_DIR)) {
+    fs.mkdirSync(CONFIGS_DIR, { recursive: true, mode: 0o700 });
+  } else {
+    fs.chmodSync(CONFIGS_DIR, 0o700);
   }
 
-  // .claude.json is only needed in per_project auth mode — in shared mode the
-  // host's ~/.claude.json is mounted directly and no project-local copy is kept.
-  if (effectiveAuthMode === "per_project") {
-    const claudeJsonPath = path.join(projectDir, ".claude.json");
-    if (!fs.existsSync(claudeJsonPath)) {
-      fs.writeFileSync(claudeJsonPath, "{}\n", { mode: 0o600 });
+  for (const dir of SHARED_DIRS) {
+    const fullPath = path.join(CONFIGS_DIR, dir);
+    if (!fs.existsSync(fullPath)) {
+      fs.mkdirSync(fullPath, { recursive: true, mode: 0o700 });
     }
   }
 
-  // Create an empty override.json as a signpost — the user knows where to add
-  // per-project overrides without having to create the file from scratch.
-  // All defaults live in ~/.code-container/config.json.
-  const overridePath = path.join(projectDir, "override.json");
-  if (!fs.existsSync(overridePath)) {
-    fs.writeFileSync(overridePath, "{}\n", { mode: 0o600 });
+  const claudeJsonPath = path.join(CONFIGS_DIR, ".claude.json");
+  if (!fs.existsSync(claudeJsonPath)) {
+    fs.writeFileSync(claudeJsonPath, "{}", { mode: 0o600 });
   }
-
-  return projectDir;
-}
-
-function _copyClaudeConfigsIfPresent(projectDir: string): void {
-  const home = os.homedir();
-
-  const srcClaudeJson = path.join(home, ".claude.json");
-  const destClaudeJson = path.join(projectDir, ".claude.json");
-  if (fs.existsSync(srcClaudeJson)) {
-    fs.copyFileSync(srcClaudeJson, destClaudeJson);
-    fs.chmodSync(destClaudeJson, 0o600);
-  }
-
-  // settings.json is intentionally NOT copied from the host.
-  // The container uses the claude_permissions from ~/.code-container/config.json,
-  // which is written to each project's settings.json by ensureProjectDir().
-}
-
-export function copyClaudeConfigs(
-  projectDirName: string,
-  authMode?: "shared" | "per_project"
-): void {
-  if ((authMode ?? loadAuthMode()) !== "per_project") return;
-  const projectDir = ensureProjectDir(projectDirName);
-  _copyClaudeConfigsIfPresent(projectDir);
 }
diff --git a/code-container/src/docker.ts b/code-container-orig/src/docker.ts
index a90e295..c02a436 100644
--- a/code-container/src/docker.ts
+++ b/code-container-orig/src/docker.ts
@@ -1,102 +1,156 @@
 import { spawnSync } from "child_process";
-import * as path from "path";
-import * as fs from "fs";
-import * as os from "os";
+import path from "path";
+import fs from "fs";
+import crypto from "crypto";
 import { printInfo, printError } from "./utils";
-import { APPDATA_DIR, DOCKERFILE_PATH, generateProjectHash, generateProjectDirName } from "./config";
+import {
+  APPDATA_DIR,
+  USER_DOCKERFILE_PATH,
+  PACKAGES_DOCKERFILE_PATH,
+} from "./config";
+import { loadMounts } from "./mounts";
+import { loadFlags, FlagSource } from "./flags";
 
 export const IMAGE_NAME = "code-container";
 export const IMAGE_TAG = "latest";
-const PACKAGED_DOCKERFILE = path.resolve(__dirname, "..", "Dockerfile");
-const PACKAGED_BASHRC = path.resolve(__dirname, "..", "container.bashrc");
-export const CONTAINER_BASHRC_PATH = path.join(APPDATA_DIR, "container.bashrc");
-const CONTAINER_PREFIX = "container";
-
-let runtimeCmd = "podman";
-
-export function setRuntimeCmd(cmd: string): void {
-  runtimeCmd = cmd;
+const CORE_IMAGE = "code-container-core";
+const PACKAGES_IMAGE = "code-container-packages";
+const BASE_IMAGE = "code-container-base";
+export type BuildTarget = "full" | "packages" | "harness" | "user";
+
+const RESOURCES_DIR = path.resolve(__dirname, "..", "resources");
+const CORE_DOCKERFILE = path.join(RESOURCES_DIR, "Dockerfile.Core");
+const HARNESS_DOCKERFILE = path.join(RESOURCES_DIR, "Dockerfile.Harness");
+const PACKAGED_PACKAGES_DOCKERFILE = path.join(
+  RESOURCES_DIR,
+  "Dockerfile.Packages",
+);
+const PACKAGED_USER_DOCKERFILE = path.join(RESOURCES_DIR, "Dockerfile.User");
+
+interface BuildStage {
+  dockerfile: string;
+  tag: string;
+  isUserFile: boolean;
+  packagedSource?: string;
 }
 
-export { generateProjectHash };
+const BUILD_STAGES: BuildStage[] = [
+  {
+    dockerfile: CORE_DOCKERFILE,
+    tag: CORE_IMAGE,
+    isUserFile: false,
+  },
+  {
+    dockerfile: PACKAGES_DOCKERFILE_PATH,
+    tag: PACKAGES_IMAGE,
+    isUserFile: true,
+    packagedSource: PACKAGED_PACKAGES_DOCKERFILE,
+  },
+  {
+    dockerfile: HARNESS_DOCKERFILE,
+    tag: BASE_IMAGE,
+    isUserFile: false,
+  },
+  {
+    dockerfile: USER_DOCKERFILE_PATH,
+    tag: IMAGE_NAME,
+    isUserFile: true,
+    packagedSource: PACKAGED_USER_DOCKERFILE,
+  },
+];
+
+const BUILD_START_INDEX: Record<BuildTarget, number> = {
+  full: 0,
+  packages: 1,
+  harness: 2,
+  user: 3,
+};
 
-export function generateContainerName(projectPath: string): string {
-  return `${CONTAINER_PREFIX}-${generateProjectDirName(projectPath)}`;
-}
+const CONTAINER_PREFIX = "container";
 
 export function checkDocker(): void {
-  const result = spawnSync(runtimeCmd, ["info"], { stdio: "pipe" });
+  const result = spawnSync("docker", ["info"], { stdio: "pipe" });
   if (result.status !== 0) {
     printError(
-      `${runtimeCmd} is not available or not running. ` +
-      `Please install or start ${runtimeCmd} before using container.`
+      "Docker is not available. Please install Docker: https://docs.docker.com/get-docker/",
     );
     process.exit(1);
   }
 }
 
+export function getMounts(projectPath: string, projectName: string): string[] {
+  const mounts: string[] = [];
+  mounts.push(`${projectPath}:/root/${projectName}`);
+  const fileMounts = loadMounts();
+  mounts.push(...fileMounts);
+  return mounts;
+}
+
+export function generateContainerName(projectPath: string): string {
+  const normalizedPath = projectPath.replace(/\/$/, "");
+  const projectName = path.basename(normalizedPath);
+  const pathHash = crypto
+    .createHash("sha1")
+    .update(normalizedPath)
+    .digest("hex")
+    .substring(0, 8);
+  return `${CONTAINER_PREFIX}-${projectName}-${pathHash}`;
+}
+
 export function imageExists(): boolean {
   const result = spawnSync(
-    runtimeCmd,
+    "docker",
     ["image", "inspect", `${IMAGE_NAME}:${IMAGE_TAG}`],
-    { stdio: "pipe" }
+    { stdio: "pipe" },
   );
   return result.status === 0;
 }
 
-export function ensureDockerfile(): void {
-  if (!fs.existsSync(DOCKERFILE_PATH)) {
-    if (fs.existsSync(PACKAGED_DOCKERFILE)) {
-      printInfo(`Dockerfile not found at ${DOCKERFILE_PATH}, copying from package...`);
-      fs.copyFileSync(PACKAGED_DOCKERFILE, DOCKERFILE_PATH);
+function ensureUserDockerfile(filePath: string, packagedSource: string): void {
+  if (!fs.existsSync(filePath)) {
+    if (fs.existsSync(packagedSource)) {
+      printInfo(`Dockerfile not found at ${filePath}, copying from package...`);
+      fs.copyFileSync(packagedSource, filePath);
     } else {
-      throw new Error(`Dockerfile not found at ${DOCKERFILE_PATH} and no packaged Dockerfile available`);
+      throw new Error(
+        `Dockerfile not found at ${filePath} and no packaged default available`,
+      );
     }
   }
 }
 
-export function ensureContainerBashrc(): void {
-  if (!fs.existsSync(CONTAINER_BASHRC_PATH)) {
-    if (fs.existsSync(PACKAGED_BASHRC)) {
-      printInfo(`container.bashrc not found at ${CONTAINER_BASHRC_PATH}, copying from package...`);
-      fs.copyFileSync(PACKAGED_BASHRC, CONTAINER_BASHRC_PATH);
-    } else {
-      throw new Error(`container.bashrc not found at ${CONTAINER_BASHRC_PATH} and no packaged file available`);
+export function buildImageRaw(target: BuildTarget): boolean {
+  const startIndex = BUILD_START_INDEX[target];
+
+  for (let i = startIndex; i < BUILD_STAGES.length; i++) {
+    const stage = BUILD_STAGES[i];
+    printInfo(`Building: ${stage.tag}`);
+
+    if (stage.isUserFile && stage.packagedSource) {
+      ensureUserDockerfile(stage.dockerfile, stage.packagedSource);
     }
-  }
-}
 
-export function buildImageRaw(baseImage: string, timezone: string, claudeVersion: string): boolean {
-  ensureDockerfile();
-  // Build context is an empty temp dir — no files need to be bundled into the image.
-  // container.bashrc is mounted at runtime rather than baked in.
-  const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), "code-container-build-"));
-  try {
     const result = spawnSync(
-      runtimeCmd,
+      "docker",
       [
         "build",
-        "-t", `${IMAGE_NAME}:${IMAGE_TAG}`,
-        "-f", DOCKERFILE_PATH,
-        "--build-arg", `BASE_IMAGE=${baseImage}`,
-        "--build-arg", `TZ=${timezone}`,
-        "--build-arg", `CLAUDE_VERSION=${claudeVersion}`,
-        tmpDir,
+        "--no-cache",
+        "-t",
+        `${stage.tag}:${IMAGE_TAG}`,
+        "-f",
+        stage.dockerfile,
+        APPDATA_DIR,
       ],
-      { stdio: "inherit" }
+      { stdio: "inherit" },
     );
-    return result.status === 0;
-  } finally {
-    try {
-      fs.rmSync(tmpDir, { recursive: true });
-    } catch {
-      // Non-fatal if cleanup fails
-    }
+    if (result.status !== 0) return false;
   }
+
+  return true;
 }
 
 export function containerExists(containerName: string): boolean {
-  const result = spawnSync(runtimeCmd, ["container", "inspect", containerName], {
+  const result = spawnSync("docker", ["container", "inspect", containerName], {
     stdio: "pipe",
   });
   return result.status === 0;
@@ -104,154 +158,122 @@ export function containerExists(containerName: string): boolean {
 
 export function containerRunning(containerName: string): boolean {
   const result = spawnSync(
-    runtimeCmd,
+    "docker",
     ["container", "inspect", "-f", "{{.State.Running}}", containerName],
-    { stdio: "pipe" }
+    { stdio: "pipe" },
   );
   return result.status === 0 && result.stdout.toString().trim() === "true";
 }
 
 export function stopContainer(containerName: string): void {
-  spawnSync(runtimeCmd, ["stop", "--timeout", "3", containerName], {
-    stdio: "inherit",
-  });
+  spawnSync("docker", ["stop", "-t", "3", containerName], { stdio: "inherit" });
 }
 
 export function startContainer(containerName: string): void {
-  spawnSync(runtimeCmd, ["start", containerName], { stdio: "inherit" });
+  spawnSync("docker", ["start", containerName], { stdio: "inherit" });
 }
 
-export function removeContainer(containerName: string): boolean {
-  const result = spawnSync(runtimeCmd, ["rm", containerName], { stdio: "inherit" });
-  return result.status === 0;
+export function removeContainer(containerName: string): void {
+  spawnSync("docker", ["rm", containerName], { stdio: "inherit" });
 }
 
 export function createNewContainer(
   containerName: string,
+  projectName: string,
   projectPath: string,
-  mounts: string[],
-  network: string = "bridge",
-  penvPath: string = "",
-  renvPath: string = "",
-  ldLibraryPath: string[] = [],
-  sshAgentContainerPath: string = ""
+  cliFlags: string[] = [],
 ): boolean {
+  const mounts = getMounts(projectPath, projectName);
   const args = ["run", "-d", "--name", containerName];
 
   args.push("-e", "TERM=xterm-256color");
   args.push("-e", "COLORTERM=truecolor");
-  args.push("-e", `PENV_PATH=${penvPath}`);
-  args.push("-e", `RENV_PATH=${renvPath}`);
-  if (ldLibraryPath.length > 0) {
-    args.push("-e", `LD_LIBRARY_PATH=${ldLibraryPath.join(":")}`);
-  }
-  if (sshAgentContainerPath) {
-    args.push("-e", `SSH_AUTH_SOCK=${sshAgentContainerPath}`);
-  }
-  args.push("-w", projectPath);
-  args.push("--network", network);
-
-  // Prevent privilege escalation via setuid/setgid binaries inside the container.
-  args.push("--security-opt", "no-new-privileges");
-
-  // Podman: preserve the host user's supplementary groups so that
-  // group-based file access on network filesystems (CephFS, NFS) works
-  // inside the container the same as outside.
-  if (runtimeCmd === "podman") {
-    args.push("--group-add", "keep-groups");
-  }
+  args.push("-w", `/root/${projectName}`);
 
   for (const mount of mounts) {
     args.push("-v", mount);
   }
 
-  // Mount container.bashrc at runtime so edits take effect without a rebuild.
-  ensureContainerBashrc();
-  args.push("-v", `${CONTAINER_BASHRC_PATH}:/etc/container.bashrc:ro`);
+  const flags = loadFlags(FlagSource.Common);
+  const runFlags = loadFlags(FlagSource.Run);
+  args.push(...flags);
+  args.push(...runFlags);
+  args.push(...cliFlags);
 
   args.push(`${IMAGE_NAME}:${IMAGE_TAG}`, "sleep", "infinity");
 
-  const result = spawnSync(runtimeCmd, args, { stdio: "inherit" });
+  const result = spawnSync("docker", args, { stdio: "inherit" });
   return result.status === 0;
 }
 
 export function execInteractive(
   containerName: string,
-  projectPath: string,
-  penvPath: string = "",
-  renvPath: string = "",
-  ldLibraryPath: string[] = [],
-  sshAgentContainerPath: string = ""
+  projectName: string,
 ): void {
-  const args = [
-    "exec", "-it",
-    "-e", "TERM=xterm-256color",
-    "-e", "COLORTERM=truecolor",
-    "-e", `PENV_PATH=${penvPath}`,
-    "-e", `RENV_PATH=${renvPath}`,
-  ];
-  if (ldLibraryPath.length > 0) {
-    args.push("-e", `LD_LIBRARY_PATH=${ldLibraryPath.join(":")}`);
-  }
-  if (sshAgentContainerPath) {
-    args.push("-e", `SSH_AUTH_SOCK=${sshAgentContainerPath}`);
-  }
-  args.push("-w", projectPath, containerName, "/bin/bash");
-  spawnSync(runtimeCmd, args, { stdio: "inherit" });
-}
-
-/**
- * File-based session tracking — more reliable than querying the container
- * runtime's ExecIDs, which Docker may not clean up immediately after an exec
- * session exits (causing stopContainerIfLastSession to leave the container
- * running when it should stop).
- *
- * Each exec session writes a lock file named by its PID. On exit, the file
- * is deleted. countActiveSessions checks for live PIDs to handle crashes.
- */
-const SESSION_PREFIX = "session-";
-
-export function trackSessionStart(sessionDir: string): string {
-  fs.mkdirSync(sessionDir, { recursive: true, mode: 0o700 });
-  const sessionFile = path.join(sessionDir, `${SESSION_PREFIX}${process.pid}`);
-  fs.writeFileSync(sessionFile, String(process.pid), { mode: 0o600 });
-  return sessionFile;
+  const flags = loadFlags(FlagSource.Common);
+  spawnSync(
+    "docker",
+    [
+      "exec",
+      "-it",
+      "-e",
+      "TERM=xterm-256color",
+      "-e",
+      "COLORTERM=truecolor",
+      "-w",
+      `/root/${projectName}`,
+      ...flags,
+      containerName,
+      "/bin/bash",
+    ],
+    { stdio: "inherit" },
+  );
 }
 
-export function trackSessionEnd(sessionFile: string): void {
-  try { fs.unlinkSync(sessionFile); } catch { /* already gone */ }
-}
+export function getOtherSessionCount(
+  containerName: string,
+  projectName: string,
+): number {
+  const result = spawnSync("ps", ["ax", "-o", "command="], {
+    encoding: "utf-8",
+  });
+  if (result.status !== 0) return 0;
 
-export function countActiveSessions(sessionDir: string): number {
-  if (!fs.existsSync(sessionDir)) return 0;
+  const lines = result.stdout.split("\n");
   let count = 0;
-  for (const file of fs.readdirSync(sessionDir)) {
-    if (!file.startsWith(SESSION_PREFIX)) continue;
-    const pid = parseInt(file.slice(SESSION_PREFIX.length), 10);
-    if (isNaN(pid)) continue;
-    try {
-      process.kill(pid, 0); // signal 0 = check if process exists, don't kill
+
+  for (const line of lines) {
+    const hasDockerExec = line.includes("docker exec");
+    const hasIt = line.includes("-it");
+    const hasContainerName = line.includes(containerName);
+    const hasBash = line.includes("/bin/bash");
+    const hasWorkdir = line.includes(`-w /root/${projectName}`);
+
+    if (hasDockerExec && hasIt && hasContainerName && hasBash && hasWorkdir) {
       count++;
-    } catch {
-      // PID not running — stale lock file from a crash; clean it up
-      try { fs.unlinkSync(path.join(sessionDir, file)); } catch { /* ignore */ }
     }
   }
+
   return count;
 }
 
-export function stopContainerIfLastSession(containerName: string, sessionDir: string): void {
-  const sessions = countActiveSessions(sessionDir);
-  if (sessions === 0) {
+export function stopContainerIfLastSession(
+  containerName: string,
+  projectName: string,
+): void {
+  const otherSessions = getOtherSessionCount(containerName, projectName);
+  if (otherSessions === 0) {
     stopContainer(containerName);
   } else {
-    printInfo(`Skipping stop; ${sessions} other session(s) still attached`);
+    printInfo(
+      `Skipping stop; ${otherSessions} other terminal(s) still attached`,
+    );
   }
 }
 
 export function listContainersRaw(): void {
   spawnSync(
-    runtimeCmd,
+    "docker",
     [
       "ps",
       "-a",
@@ -260,13 +282,13 @@ export function listContainersRaw(): void {
       "--format",
       "table {{.Names}}\t{{.Status}}\t{{.CreatedAt}}",
     ],
-    { stdio: "inherit" }
+    { stdio: "inherit" },
   );
 }
 
 export function getStoppedContainerIds(): string[] {
   const result = spawnSync(
-    runtimeCmd,
+    "docker",
     [
       "ps",
       "-a",
@@ -276,14 +298,15 @@ export function getStoppedContainerIds(): string[] {
       "status=exited",
       "--quiet",
     ],
-    { encoding: "utf8" }
+    { encoding: "utf8" },
   );
 
   const containerIds = result.stdout.trim();
   if (!containerIds) return [];
+
   return containerIds.split("\n");
 }
 
 export function removeContainersById(ids: string[]): void {
-  spawnSync(runtimeCmd, ["rm", ...ids], { stdio: "inherit" });
+  spawnSync("docker", ["rm", ...ids], { stdio: "inherit" });
 }
diff --git a/code-container-orig/src/flags.ts b/code-container-orig/src/flags.ts
new file mode 100644
index 0000000..963ed3c
--- /dev/null
+++ b/code-container-orig/src/flags.ts
@@ -0,0 +1,53 @@
+import fs from "fs";
+import { parse } from "shell-quote";
+
+import { FLAGS_PATH, RUN_FLAGS_PATH } from "./config";
+import { printError } from "./utils";
+
+export enum FlagSource {
+  Common = "common",
+  Run = "run",
+}
+
+function loadFlagsFromPath(filePath: string): string[] {
+  if (!fs.existsSync(filePath)) {
+    return [];
+  }
+  const content = fs.readFileSync(filePath, "utf-8");
+  const flags: string[] = [];
+
+  for (const line of content.split("\n")) {
+    const trimmed = line.trim();
+    if (!trimmed || trimmed.startsWith("#")) {
+      continue;
+    }
+
+    const parts = parse(trimmed);
+    const lineFlags: string[] = [];
+    let hasOperator = false;
+
+    for (const part of parts) {
+      if (typeof part === "string") {
+        lineFlags.push(part);
+      } else {
+        hasOperator = true;
+        break;
+      }
+    }
+
+    if (hasOperator) {
+      printError("Invalid Docker flag line: shell operators are not allowed.");
+      printError(`Argument skipped: ${line}`);
+      continue;
+    }
+
+    flags.push(...lineFlags);
+  }
+
+  return flags;
+}
+
+export function loadFlags(source: FlagSource): string[] {
+  const filePath = source === FlagSource.Run ? RUN_FLAGS_PATH : FLAGS_PATH;
+  return loadFlagsFromPath(filePath);
+}
diff --git a/code-container/src/main.ts b/code-container-orig/src/main.ts
index 517fb74..7089501 100644
--- a/code-container/src/main.ts
+++ b/code-container-orig/src/main.ts
@@ -1,6 +1,6 @@
 #!/usr/bin/env node
 
-import { printError, printInfo, promptYesNo, resolveProjectPath } from "./utils";
+import { printInfo, promptYesNo, resolveProjectPath } from "./utils";
 import {
   buildImage,
   runContainer,
@@ -10,9 +10,10 @@ import {
   cleanContainers,
   init,
 } from "./commands";
-import { checkDocker, setRuntimeCmd } from "./docker";
+import { checkDocker } from "./docker";
 import { loadSettings, saveSettings } from "./config";
-import { ensureHostConfig, loadGlobalConfig } from "./mount-config";
+import { ensureMountsFile } from "./mounts";
+import { parseArgs } from "./args";
 
 const TOS = `
 \x1b[33m⚠️  Security Advisory:\x1b[0m
@@ -21,18 +22,22 @@ The main purpose of Code Container is to protect commands like 'rm' or 'apt'
 from unintentionally affecting your main system.
 
 container does not protect from prompt injections in the event that an agent
-becomes misaligned. This is an innate problem within coding harness software
-and container does not attempt to solve it.
-
-Users are advised not to work with unverified software inside the container.
-- Sensitive information inside the container may still be accessible by
-  an agent, including:
-  - OAuth credentials and API keys in harness configs
-  - Any files visible through mounted read-only paths
-
-By using Code Container, you agree that you are aware of these risks and
-will not hold the author liable for any outcomes arising from usage of
-the software.
+becomes malaligned.
+
+This is an innate problem within coding harness software and container does
+not attempt to solve it.
+
+Users are advised to not download or work with unverified software.
+- Sensitive information inside the container may still be exfiltrated by
+  an attacker just as with your regular system.
+  - This includes:
+  - OAuth credentials inside harness configs
+  - API keys inside harness configs
+  - SSH keys for git functionality if enabled
+
+Never install or run your harness on unverified software. By using Code
+Container, you agree that you are aware of these risks and will not hold the
+author liable for any outcomes arising from usage of the software.
 `;
 
 async function ensureTosAccepted(): Promise<boolean> {
@@ -51,100 +56,25 @@ async function ensureTosAccepted(): Promise<boolean> {
   return false;
 }
 
-function usage(): void {
-  console.log(`
-Usage: container [COMMAND] [PROJECT_PATH]
-
-Manage Code containers for isolated project environments.
-
-Commands:
-    (none)         Start container for current directory (default)
-    run            Start container for specified project path
-    build          Build the container image
-    init           Copy Claude configs from host into this project's config dir
-    stop           Stop the container for this project
-    remove         Remove the container for this project
-    list           List all Code containers
-    clean          Remove all stopped Code containers
-
-Arguments:
-    PROJECT_PATH    Path to the project directory (defaults to current directory)
-
-Examples:
-    container                           # Start container for current directory
-    container run /path/to/project      # Start container for specific project
-    container build                     # Build container image
-    container init                      # Copy/refresh Claude configs for current project
-    container stop                      # Stop container for current directory
-    container remove /path/to/project   # Remove container for specific project
-    container list                      # List all containers
-    container clean                     # Clean up stopped containers
-
-Configuration:
-    ~/.code-container/config.json                          # Global defaults (all projects)
-    ~/.code-container/projects/<name>-<hash>/override.json # Per-project overrides
-
-    Config options:
-      network              Docker/Podman network mode (default: "bridge")
-      extra_readonly       Additional read-only paths to mount
-      extra_readwrite      Additional read-write paths to mount
-      extra_ld_library_path  Directories to mount ro and prepend to LD_LIBRARY_PATH
-      project_symlink_mounts  Auto-mount symlink targets in project tree ("read"/"write"/"off")
-`);
-  process.exit(0);
-}
-
 async function main(): Promise<void> {
-  const args = process.argv.slice(2);
-  let command = "";
-  let projectPath = "";
-
-  if (args.length > 0) {
-    const firstArg = args[0];
-    if (firstArg === "help" || firstArg === "--help" || firstArg === "-h") {
-      usage();
-    }
-
-    const validCommands = [
-      "run",
-      "build",
-      "init",
-      "stop",
-      "remove",
-      "list",
-      "clean",
-    ];
-    if (validCommands.includes(firstArg)) {
-      command = firstArg;
-      if (args.length > 2) {
-        printError(`Unexpected argument: ${args[2]}`);
-        usage();
-      }
-      projectPath = args[1] ?? "";
-    } else {
-      printError(`Unknown command: ${firstArg}`);
-      usage();
-    }
-  }
+  const parsed = parseArgs(process.argv.slice(2));
 
-  if (!await ensureTosAccepted()) {
+  if (!(await ensureTosAccepted())) {
     printInfo("Terms not accepted. Exiting...");
     process.exit(1);
   }
 
-  ensureHostConfig();
-  setRuntimeCmd(loadGlobalConfig().container_runtime);
-
-  const resolvedPath = resolveProjectPath(projectPath);
+  await ensureMountsFile();
 
-  if (command === "init") {
-    init(resolvedPath);
+  if (parsed.command === "init") {
+    await init();
     return;
   }
 
   checkDocker();
+  await init(true);
 
-  switch (command) {
+  switch (parsed.command) {
     case "list":
       listContainers();
       return;
@@ -152,18 +82,23 @@ async function main(): Promise<void> {
       cleanContainers();
       return;
     case "build":
-      buildImage();
+      buildImage(parsed.target);
       return;
-    case "stop":
-      stopContainerForProject(resolvedPath);
+    case "stop": {
+      const resolved = resolveProjectPath(parsed.projectPath);
+      stopContainerForProject(resolved);
       return;
-    case "remove":
-      removeContainerForProject(resolvedPath);
+    }
+    case "remove": {
+      const resolved = resolveProjectPath(parsed.projectPath);
+      removeContainerForProject(resolved);
       return;
-    case "run":
-    case "":
-      await runContainer(resolvedPath);
+    }
+    case "run": {
+      const resolved = resolveProjectPath(parsed.projectPath);
+      await runContainer(resolved, parsed.cliFlags);
       return;
+    }
   }
 }
 
diff --git a/code-container/src/mount-config.ts b/code-container/src/mount-config.ts
deleted file mode 100644
index 63da31d..0000000
--- a/code-container/src/mount-config.ts
+++ /dev/null
@@ -1,140 +0,0 @@
-import * as fs from "fs";
-import * as path from "path";
-import { z } from "zod";
-import { APPDATA_DIR, PROJECTS_DIR } from "./config";
-
-export const HOST_CONFIG_PATH = path.join(APPDATA_DIR, "config.json");
-
-// Global host-level configuration (~/.code-container/config.json).
-// Never mounted into containers — cannot be modified from inside a container.
-// All configurable fields live here. This is the single source of global defaults.
-// Fields marked "global only" cannot be overridden per-project.
-// All other fields can be overridden in the per-project override.json.
-const GlobalMountConfigSchema = z.object({
-  base_image:          z.string().default("rockylinux:9"),   // build-time, global only
-  timezone:            z.string().default("UTC"),             // build-time, global only
-  claude_version:      z.string().default(""),                // build-time, global only
-  container_runtime:   z.enum(["podman", "docker"]).default("podman"), // global only
-  data_branches:       z.array(z.string()).default([]),        // global only
-  network:             z.string().default("bridge"),
-  keep_alive:          z.boolean().default(false),
-  mount_home_children: z.boolean().default(true),
-  claude_auth_mode:    z.enum(["shared", "per_project"]).default("shared"),
-  claude_history_mode: z.enum(["shared", "isolated"]).default("shared"),
-  penv_path:           z.string().default(""),
-  renv_path:           z.string().default(""),
-  extra_readonly:      z.array(z.string()).default([]),
-  extra_readwrite:     z.array(z.string()).default([]),
-  extra_ld_library_path: z.array(z.string()).default([]),
-  project_symlink_mounts: z.enum(["read", "write", "off"]).default("read"),
-  project_symlink_depth:  z.number().int().min(1).default(3),
-  forward_ssh_agent:      z.boolean().default(false),
-  ssh_known_hosts_path:   z.string().default(""),
-  claude_permissions:  z.object({                             // default for new project settings.json
-    allow: z.array(z.string()).default(["*"]),
-    deny:  z.array(z.string()).default([]),
-  }).default({ allow: ["*"], deny: [] }),
-});
-
-// Per-project overrides (~/.code-container/projects/<hash>/override.json).
-// All fields are optional — absence means inherit from config.json.
-// Created as {} on first project run; add only what you want to override.
-const ProjectOverrideSchema = z.object({
-  network: z.string().optional(),
-  keep_alive: z.boolean().optional(),
-  project_readonly: z.boolean().optional(),
-  claude_auth_mode: z.enum(["shared", "per_project"]).optional(),
-  claude_history_mode: z.enum(["shared", "isolated"]).optional(),
-  mount_home_children: z.boolean().optional(),
-  penv_path: z.string().optional(),
-  renv_path: z.string().optional(),
-  extra_readonly: z.array(z.string()).optional(),
-  extra_readwrite: z.array(z.string()).optional(),
-  extra_ld_library_path: z.array(z.string()).optional(),
-  project_symlink_mounts: z.enum(["read", "write", "off"]).optional(),
-  project_symlink_depth: z.number().int().min(1).optional(),
-  forward_ssh_agent: z.boolean().optional(),
-  ssh_known_hosts_path: z.string().optional(),
-});
-
-export type GlobalConfig = z.infer<typeof GlobalMountConfigSchema>;
-
-export type MountConfig = {
-  claude_auth_mode: "shared" | "per_project";
-  claude_history_mode: "shared" | "isolated";
-  network: string;
-  keep_alive: boolean;
-  project_readonly: boolean;
-  penv_path: string;
-  renv_path: string;
-  data_branches: string[];
-  mount_home_children: boolean;
-  extra_readonly: string[];
-  extra_readwrite: string[];
-  extra_ld_library_path: string[];
-  project_symlink_mounts: "read" | "write" | "off";
-  project_symlink_depth: number;
-  forward_ssh_agent: boolean;
-  ssh_known_hosts_path: string;
-};
-
-function parseFile<T extends z.ZodTypeAny>(schema: T, filePath: string): z.infer<T> {
-  if (!fs.existsSync(filePath)) return schema.parse({});
-  let raw: string;
-  try { raw = fs.readFileSync(filePath, "utf-8"); }
-  catch { return schema.parse({}); }
-  let json: unknown;
-  try { json = JSON.parse(raw); }
-  catch {
-    console.error(`[code-container] Warning: ${filePath} contains invalid JSON — using schema defaults`);
-    return schema.parse({});
-  }
-  try { return schema.parse(json); }
-  catch {
-    console.error(`[code-container] Warning: ${filePath} has unexpected field types or values — using schema defaults`);
-    return schema.parse({});
-  }
-}
-
-export function loadGlobalConfig(): GlobalConfig {
-  return parseFile(GlobalMountConfigSchema, HOST_CONFIG_PATH);
-}
-
-export function loadMountConfig(projectDirName: string): MountConfig {
-  const global = loadGlobalConfig();
-  const override = parseFile(
-    ProjectOverrideSchema,
-    path.join(PROJECTS_DIR, projectDirName, "override.json")
-  );
-
-  return {
-    claude_auth_mode: override.claude_auth_mode ?? global.claude_auth_mode,
-    claude_history_mode: override.claude_history_mode ?? global.claude_history_mode,
-    network: override.network ?? global.network,
-    keep_alive: override.keep_alive ?? global.keep_alive,
-    project_readonly: override.project_readonly ?? false,
-    penv_path: override.penv_path ?? global.penv_path,
-    renv_path: override.renv_path ?? global.renv_path,
-    data_branches: global.data_branches,
-    mount_home_children: override.mount_home_children ?? global.mount_home_children,
-    extra_readonly: [...global.extra_readonly, ...(override.extra_readonly ?? [])],
-    extra_readwrite: [...global.extra_readwrite, ...(override.extra_readwrite ?? [])],
-    extra_ld_library_path: [...(global.extra_ld_library_path ?? []), ...(override.extra_ld_library_path ?? [])],
-    project_symlink_mounts: override.project_symlink_mounts ?? global.project_symlink_mounts,
-    project_symlink_depth: override.project_symlink_depth ?? global.project_symlink_depth,
-    forward_ssh_agent: override.forward_ssh_agent ?? global.forward_ssh_agent,
-    ssh_known_hosts_path: override.ssh_known_hosts_path ?? global.ssh_known_hosts_path,
-  };
-}
-
-export function ensureHostConfig(): void {
-  if (!fs.existsSync(HOST_CONFIG_PATH)) {
-    const defaults = GlobalMountConfigSchema.parse({});
-    fs.writeFileSync(
-      HOST_CONFIG_PATH,
-      JSON.stringify(defaults, null, 2) + "\n",
-      { mode: 0o600 }
-    );
-  }
-}
-
diff --git a/code-container/src/mounts.ts b/code-container-orig/src/mounts.ts
index feb80ee..fba6e76 100644
--- a/code-container/src/mounts.ts
+++ b/code-container-orig/src/mounts.ts
@@ -1,440 +1,73 @@
-import * as fs from "fs";
-import * as path from "path";
-import * as os from "os";
-import { PROJECTS_DIR, APPDATA_DIR } from "./config";
-import { printWarning } from "./utils";
-import type { MountConfig } from "./mount-config";
+import fs from "fs";
+import os from "os";
+import { CONFIGS_DIR, MOUNTS_PATH, ensureAppdataDir } from "./config";
+import { printInfo, promptYesNo } from "./utils";
 
-/**
- * If penvPath is a venv whose Python interpreter symlinks outside the
- * already-planned mounts (common with uv, pyenv, asdf, conda managed envs),
- * return a mount spec to add as a read-only mount.
- *
- * Uses readlinkSync (one level only) rather than realpathSync so that the
- * container destination path matches what the symlink actually expects.
- * The host source path is then resolved via realpathSync so we bind-mount
- * real content even when the symlink target itself contains further symlinks
- * (e.g. uv's cpython-3.13 -> cpython-3.13.12, or /home/user -> /real/path).
- *
- * Returns a mount spec string "hostPath:containerPath:ro", which may be
- * asymmetric (source ≠ destination) when the symlink traverses paths that
- * differ between host and container namespaces.
- */
-function resolveVenvInterpreterMount(
-  penvPath: string,
-  plannedMountRoots: Set<string>
-): string | null {
-  if (!penvPath) return null;
-
-  const activateScript = path.join(penvPath, "bin", "activate");
-  const pythonBin = path.join(penvPath, "bin", "python");
-  if (!fs.existsSync(activateScript) || !fs.existsSync(pythonBin)) return null;
-
-  // Read the immediate symlink target without recursively resolving.
-  // This preserves the path the container will traverse (e.g. /home/user/...
-  // rather than the fully-resolved /real/ceph/path/...).
-  let symlinkTarget: string;
-  try {
-    const raw = fs.readlinkSync(pythonBin);
-    symlinkTarget = path.isAbsolute(raw)
-      ? raw
-      : path.resolve(path.dirname(pythonBin), raw);
-  } catch {
-    // pythonBin is not a symlink — interpreter lives inside the venv, no extra mount needed.
-    return null;
-  }
-
-  // The path the container needs: two levels above the target binary (<prefix>/bin/pythonX.Y).
-  const containerInterpreterPath = path.dirname(path.dirname(symlinkTarget));
-
-  // Already covered by a planned mount — nothing to do.
-  for (const root of plannedMountRoots) {
-    if (containerInterpreterPath === root ||
-        containerInterpreterPath.startsWith(root + path.sep)) return null;
-  }
-
-  // Resolve the container path to a real host path (follows any remaining symlinks
-  // so we bind-mount actual content, not a dangling path).
-  let hostInterpreterPath: string;
-  try {
-    hostInterpreterPath = fs.realpathSync(containerInterpreterPath);
-  } catch {
-    return null;
-  }
-
-  if (!fs.existsSync(hostInterpreterPath)) return null;
-
-  // Asymmetric when host and container paths differ (symlinks in the path were resolved).
-  return `${hostInterpreterPath}:${containerInterpreterPath}:ro`;
+function getCoreMounts(): string[] {
+  const home = os.homedir();
+  return [
+    `${CONFIGS_DIR}/.claude:/root/.claude`,
+    `${CONFIGS_DIR}/.claude.json:/root/.claude.json`,
+    `${CONFIGS_DIR}/.codex:/root/.codex`,
+    `${CONFIGS_DIR}/.copilot:/root/.copilot`,
+    `${CONFIGS_DIR}/.opencode:/root/.config/opencode`,
+    `${CONFIGS_DIR}/.gemini:/root/.gemini`,
+    `${CONFIGS_DIR}/.local/share:/root/.local/share`,
+    `${CONFIGS_DIR}/.local/state:/root/.local/state`,
+    `${home}/.gitconfig:/root/.gitconfig:ro`,
+  ];
 }
 
-/**
- * Walk a directory tree up to maxDepth levels deep, invoking callback for
- * each symlink found. Hidden directories (names starting with ".") are skipped
- * to avoid scanning .git, .ssh, etc. Does not recurse into symlinked directories.
- */
-function walkForSymlinks(
-  dir: string,
-  maxDepth: number,
-  callback: (realTarget: string, symlinkPath: string) => void,
-  currentDepth: number = 0
-): void {
-  if (currentDepth >= maxDepth) return;
-  let entries: fs.Dirent[];
-  try {
-    entries = fs.readdirSync(dir, { withFileTypes: true });
-  } catch {
+export async function ensureMountsFile(): Promise<void> {
+  if (fs.existsSync(MOUNTS_PATH)) {
     return;
   }
-  for (const entry of entries) {
-    if (entry.name.startsWith(".")) continue;
-    const fullPath = path.join(dir, entry.name);
-    if (entry.isSymbolicLink()) {
-      try {
-        const raw = fs.readlinkSync(fullPath);
-        const abs = path.isAbsolute(raw)
-          ? raw
-          : path.resolve(path.dirname(fullPath), raw);
-        const real = (() => { try { return fs.realpathSync(abs); } catch { return abs; } })();
-        callback(real, fullPath);
-      } catch {
-        // dead or unreadable link — skip
-      }
-    } else if (entry.isDirectory()) {
-      walkForSymlinks(fullPath, maxDepth, callback, currentDepth + 1);
-    }
-  }
-}
 
-/**
- * Build the full mount list for a container. Order matters:
- * all read-only mounts come first; read-write mounts (project path and
- * extra_readwrite) come last so they shadow any read-only parent mount.
- */
-export function buildMounts(
-  projectPath: string,
-  projectDirName: string,
-  mountConfig: MountConfig
-): string[] {
-  const mounts: string[] = [];
-  // Map from container destination path to index in mounts[] — allows
-  // read-write mounts to upgrade earlier read-only mounts for the same path
-  // (e.g. a home child symlink whose target is also in extra_readwrite).
-  const mountIndex = new Map<string, number>();
-  const hostPathSet = new Set<string>(); // host-side paths already covered by a mount
+  ensureAppdataDir();
   const home = os.homedir();
+  const mounts: string[] = [];
 
-  function addMount(spec: string): void {
-    const parts = spec.split(":");
-    // Deduplicate by container destination path — the same host path may
-    // legitimately be mounted at two different container destinations (e.g.
-    // ~/.claude.json mounted at its original path via home-children AND at
-    // /root/.claude.json for Claude's auth).
-    const containerPath = parts[1] ?? parts[0];
-    const isReadOnly = parts[2] === "ro";
-
-    if (mountIndex.has(containerPath)) {
-      if (!isReadOnly) {
-        // A read-write mount upgrades a prior read-only mount for the same
-        // container destination so writes through the path are not blocked.
-        const idx = mountIndex.get(containerPath)!;
-        if (mounts[idx].endsWith(":ro")) {
-          mounts[idx] = spec;
-        }
-      }
-      return;
-    }
-    mountIndex.set(containerPath, mounts.length);
-    hostPathSet.add(parts[0]);
-    mounts.push(spec);
-  }
-
-  // Returns true if p is already covered by an existing mount (exact match
-  // or a mounted ancestor). Uses live mountIndex so callers see up-to-date state.
-  // Checks whether host path p is already covered by a planned mount.
-  // Uses hostPathSet only — mountIndex holds container destination paths which
-  // differ from host paths for asymmetric mounts and must not be checked here.
-  function isCovered(p: string): boolean {
-    for (const h of hostPathSet) {
-      if (p === h || p.startsWith(h + "/")) return true;
-    }
-    return false;
-  }
-
-  // Parse an entry from extra_readonly / extra_readwrite.
-  // Entries may be a plain path (symmetric mount) or "hostPath:containerPath"
-  // (asymmetric — useful for mounting host files at a different container path,
-  // e.g. "/home/user/.foo:/root/.foo").
-  // NOTE: paths containing a colon character are not supported as entries in
-  // extra_readonly / extra_readwrite — the first colon is always treated as the
-  // host:container separator. Such paths are valid on Linux but are uncommon.
-  function parseMountEntry(entry: string): { hostPath: string; containerPath: string } {
-    const idx = entry.indexOf(":");
-    if (idx < 0) return { hostPath: entry, containerPath: entry };
-    return { hostPath: entry.slice(0, idx), containerPath: entry.slice(idx + 1) };
-  }
-
-  // Add a path as read-write, first protecting its HOST ancestor directories
-  // with read-only mounts. Without this, Podman exposes the parent directories
-  // of a bind-mounted path from the host with no access restrictions.
-  // Always protects the immediate parent (an rw bind mount disrupts the ro
-  // coverage its parent would otherwise inherit from an ancestor ro mount).
-  // Stops walking up once a grandparent is already covered by an existing mount.
-  // containerPath may differ from hostPath for asymmetric mounts.
-  function addWriteWithProtection(hostPath: string, containerPath: string = hostPath): void {
-    let ancestor = path.dirname(hostPath);
-    let isImmediateParent = true;
-    // The filesystem root `/` is never protected — the container has its own rootfs
-    // so the host root is not visible. The loop stops just before reaching it.
-    while (ancestor !== path.dirname(ancestor)) {
-      if (!isImmediateParent && isCovered(ancestor)) break;
-      if (fs.existsSync(ancestor)) {
-        addMount(`${ancestor}:${ancestor}:ro`);
-      }
-      isImmediateParent = false;
-      ancestor = path.dirname(ancestor);
-    }
-    addMount(`${hostPath}:${containerPath}`);
-  }
-
-  // Configured data branches, read-only
-  for (const branch of mountConfig.data_branches) {
-    if (fs.existsSync(branch)) {
-      addMount(`${branch}:${branch}:ro`);
-    }
-  }
-
-  // Well-known language toolchain hidden directories — auto-mounted read-only
-  // alongside mount_home_children. These are the exception to the rule that
-  // hidden dirs are never mounted: language version managers install runtimes
-  // here and agents need them to build and run code. Credential and config
-  // dirs (.ssh, .aws, .gnupg, .config, etc.) remain excluded.
-  // Mount specific subdirectories rather than whole dirs where the parent
-  // contains sensitive files (e.g. ~/.cargo/credentials.toml, ~/.local/share).
-  // ~/.local is mounted in full rather than as selective subdirs because
-  // ~/.local/bin commonly contains symlinks into ~/.local/share (e.g. pipx
-  // installs), which would be dead links if ~/.local/share were excluded.
-  // ~/.local/share/keyrings is encrypted and not useful read-only; other
-  // ~/.local/share content is app state that is acceptable to expose read-only.
-  const TOOLCHAIN_HIDDEN_PATHS = [
-    ".nvm",    // Node Version Manager — node, npm, npx per-version binaries
-    ".cargo",  // Rust — cargo binary, registry, env (credentials.toml is noted in security docs)
-    ".rustup", // Rust toolchain — rustc, std libs, cross-compilation targets
-    ".local",  // User-local installs — ~/.local/bin, ~/.local/lib, pip install --user, etc.
-    ".pyenv",  // Python version manager
-  ];
-
-  if (mountConfig.mount_home_children && fs.existsSync(home)) {
-    for (const name of TOOLCHAIN_HIDDEN_PATHS) {
-      const dirPath = path.join(home, name);
-      if (fs.existsSync(dirPath)) {
-        addMount(`${dirPath}:${dirPath}:ro`);
-      }
-    }
-  }
-
-  // Non-hidden children of $HOME, read-only.
-  // All other hidden dirs (.ssh, .aws, .gnupg, .config, etc.) are never mounted.
-  // Enumerated at container creation time — new dirs added later are not visible
-  // until the container is removed and recreated.
-  if (mountConfig.mount_home_children && fs.existsSync(home)) {
-    // Resolve all explicitly configured paths upfront for symlink target checks.
-    function resolveAll(paths: string[]): string[] {
-      return paths
-        .filter(p => { try { return fs.existsSync(p); } catch { return false; } })
-        .map(p => { try { return fs.realpathSync(p); } catch { return p; } });
-    }
-    const hostOnly = (entries: string[]) => entries.map(e => parseMountEntry(e).hostPath);
-    const writableRealPaths  = resolveAll([projectPath, ...hostOnly(mountConfig.extra_readwrite)]);
-    const configuredRealPaths = resolveAll([
-      projectPath,
-      ...mountConfig.data_branches,
-      ...hostOnly(mountConfig.extra_readonly),
-      ...hostOnly(mountConfig.extra_readwrite),
-    ]);
-
-    function isUnder(target: string, roots: string[]): boolean {
-      return roots.some(r => target === r || target.startsWith(r + path.sep));
-    }
-
-    try {
-      const children = fs.readdirSync(home).filter(name => !name.startsWith("."));
-      for (const child of children) {
-        const childPath = path.join(home, child);
-
-        let lstat: fs.Stats;
-        try { lstat = fs.lstatSync(childPath); } catch { continue; }
-
-        if (lstat.isSymbolicLink()) {
-          let realTarget: string;
-          try { realTarget = fs.realpathSync(childPath); } catch { continue; }
-
-          // Always skip symlinks to ~/.code-container/.
-          if (realTarget === APPDATA_DIR || realTarget.startsWith(APPDATA_DIR + path.sep)) {
-            continue;
-          }
-
-          if (realTarget.startsWith(home + path.sep)) {
-            // Symlink resolves inside $HOME: skip if it points into a hidden dir.
-            // e.g. ~/view-ssh -> ~/.ssh would otherwise expose SSH keys.
-            const firstComponent = path.relative(home, realTarget).split(path.sep)[0];
-            if (firstComponent.startsWith(".")) continue;
-            // Non-hidden under home: fall through to mount normally.
-          } else {
-            // Symlink resolves outside $HOME: only mount if the target is under
-            // an explicitly configured path (data_branches, extra_readonly,
-            // extra_readwrite, or project path). Anything else is not intentionally
-            // exposed and should remain inaccessible.
-            if (!isUnder(realTarget, configuredRealPaths)) continue;
-          }
-
-          // Mount read-write if the target is a configured writable path,
-          // so that writes through the symlink are not blocked by a read-only mount.
-          if (isUnder(realTarget, writableRealPaths)) {
-            addMount(`${childPath}:${childPath}`);
-            continue;
-          }
-        }
-
-        addMount(`${childPath}:${childPath}:ro`);
-      }
-    } catch {
-      // If home is unreadable, skip enumeration silently
-    }
-  }
-
-  // Extra read-only paths from host config (global + per-project).
-  // Entries may be "path" (symmetric) or "hostPath:containerPath" (asymmetric).
-  for (const extra of mountConfig.extra_readonly) {
-    const { hostPath, containerPath } = parseMountEntry(extra);
-    if (fs.existsSync(hostPath)) {
-      addMount(`${hostPath}:${containerPath}:ro`);
-    }
-  }
-
-  // LD_LIBRARY_PATH entries are auto-mounted read-only
-  for (const extra of mountConfig.extra_ld_library_path) {
-    if (fs.existsSync(extra)) {
-      addMount(`${extra}:${extra}:ro`);
-    }
-  }
-
-  // Auto-mount the interpreter root when penv_path is a venv whose Python
-  // symlinks outside the planned mounts (e.g. uv-managed interpreters in
-  // ~/.local/share/uv/python/). Resolves the symlink chain and mounts the
-  // prefix directory two levels above the real binary, read-only.
-  const venvInterpreterMount = resolveVenvInterpreterMount(mountConfig.penv_path, new Set(mountIndex.keys()));
-  if (venvInterpreterMount) {
-    addMount(venvInterpreterMount);
-  }
-
-  // Auto-mount renv_path if set and not already covered by an existing mount.
-  // R environments don't use the same interpreter-symlink pattern as Python venvs,
-  // but the renv directory itself must be accessible for `act` to source it.
-  if (mountConfig.renv_path && fs.existsSync(mountConfig.renv_path)) {
-    if (!isCovered(mountConfig.renv_path)) {
-      addMount(`${mountConfig.renv_path}:${mountConfig.renv_path}:ro`);
-    }
-  }
-
-  // Read-write mounts come last so they shadow any parent read-only mount.
-
-  // Project path — read-write by default, read-only if project_readonly is set
-  addMount(mountConfig.project_readonly
-    ? `${projectPath}:${projectPath}:ro`
-    : `${projectPath}:${projectPath}`);
-
-  // Extra writable paths — add with ancestor ro protection.
-  // Entries may be "path" (symmetric) or "hostPath:containerPath" (asymmetric).
-  for (const extra of mountConfig.extra_readwrite) {
-    const { hostPath, containerPath } = parseMountEntry(extra);
-    if (fs.existsSync(hostPath)) {
-      addWriteWithProtection(hostPath, containerPath);
-    }
-  }
-
-  // Auto-mount targets of symlinks found within the project directory tree.
-  // Avoids dead links for symlinks that point outside the project path.
-  // "read"  — mount targets read-only
-  // "write" — mount targets read-write with ancestor ro protection
-  // "off"   — disabled
-  if (mountConfig.project_symlink_mounts !== "off" && fs.existsSync(projectPath)) {
-    const isWrite = mountConfig.project_symlink_mounts === "write";
-    walkForSymlinks(projectPath, mountConfig.project_symlink_depth, (realTarget, symlinkPath) => {
-      if (realTarget === APPDATA_DIR || realTarget.startsWith(APPDATA_DIR + path.sep)) return;
-      if (!fs.existsSync(realTarget)) {
-        printWarning(`Symlink target not accessible, skipping mount: ${symlinkPath} -> ${realTarget}`);
-        return;
-      }
-      if (isCovered(realTarget)) return;
-      if (isWrite) {
-        addWriteWithProtection(realTarget);
-      } else {
-        addMount(`${realTarget}:${realTarget}:ro`);
-      }
-    });
-  }
-
-  // SSH agent relay — mount the relay directory so the stable socket inside it
-  // is accessible inside the container. The relay process on the host proxies
-  // connections to whatever the current SSH_AUTH_SOCK is, so the container
-  // never needs to be recreated when SSH reconnects.
-  // Also mount known_hosts so SSH can verify host keys when using the agent
-  // (e.g. git push over SSH). known_hosts contains only public keys and is
-  // not sensitive.
-  if (mountConfig.forward_ssh_agent) {
-    const relayDir = path.join(home, ".ssh-agent-relay");
-    if (fs.existsSync(relayDir)) {
-      addMount(`${relayDir}:${relayDir}:ro`);
-    }
-    if (mountConfig.ssh_known_hosts_path && fs.existsSync(mountConfig.ssh_known_hosts_path)) {
-      addMount(`${mountConfig.ssh_known_hosts_path}:/root/.ssh/known_hosts:ro`);
-    }
-  }
-
-  // Claude config — settings.json read-only (agent cannot self-modify permissions)
-  const projectDir = path.join(PROJECTS_DIR, projectDirName);
-  addMount(`${path.join(projectDir, "settings.json")}:/root/.claude/settings.json:ro`);
+  printInfo("");
+  printInfo("MOUNTS.txt not found. Creating....");
+  printInfo("");
+  printInfo("Would you like to mount ~/.ssh (read-only)?");
+  printInfo(
+    "  Pros: Enables SSH-based git operations and remote server access inside the container. (E.g.: git push, git pull)",
+  );
+  printInfo(
+    "  Risks: Exposes your SSH private keys. Only enable if you trust the code running in your containers.",
+  );
+  printInfo(
+    "  Note: This configuration is global. You may modify your mounts at any time by editing ~/.code-container/MOUNTS.txt.",
+  );
+
+  const mountSsh = await promptYesNo("Mount ~/.ssh?");
+  if (mountSsh) {
+    mounts.push(`${home}/.ssh:/root/.ssh:ro`);
+  }
+
+  fs.writeFileSync(MOUNTS_PATH, mounts.join("\n") + "\n", { mode: 0o600 });
+  printInfo("");
+  printInfo(`Created ${MOUNTS_PATH}`);
+  printInfo(
+    "Core mounts are always applied. Modify this file to store additional mount points.",
+  );
+}
 
-  // .claude.json auth credentials.
-  // "shared" (default): mount ~/.claude.json from the host read-write — Claude writes
-  //   to this file on startup (startup counter, token refresh).
-  // "per_project": mount a per-project copy read-write — required for OAuth where
-  //   token refreshes must write back. Set claude_auth_mode in config.json to enable.
-  if (mountConfig.claude_auth_mode === "shared") {
-    const hostClaudeJson = path.join(home, ".claude.json");
-    if (fs.existsSync(hostClaudeJson)) {
-      addMount(`${hostClaudeJson}:/root/.claude.json`);
-    }
-  } else {
-    addMount(`${path.join(projectDir, ".claude.json")}:/root/.claude.json`);
-  }
+export function loadMounts(): string[] {
+  const coreMounts = getCoreMounts();
+  const mountSet = new Set(coreMounts);
 
-  // Conversation history.
-  // "shared" (default): mount host ~/.claude/projects directly so history is
-  //   continuous across host sessions and all containers for the same project.
-  //   Claude Code namespaces by project path hash so projects don't bleed into
-  //   each other. Set claude_history_mode to "isolated" to opt out.
-  // "isolated": mount a per-container copy so each container starts with a
-  //   clean slate. Useful when you specifically want no history carryover.
-  if (mountConfig.claude_history_mode === "shared") {
-    const hostClaudeProjects = path.join(home, ".claude", "projects");
-    if (!fs.existsSync(hostClaudeProjects)) {
-      fs.mkdirSync(hostClaudeProjects, { recursive: true });
+  if (fs.existsSync(MOUNTS_PATH)) {
+    const content = fs.readFileSync(MOUNTS_PATH, "utf-8");
+    const extraMounts = content
+      .split("\n")
+      .map((line) => line.trim())
+      .filter((line) => line && !line.startsWith("#"));
+    for (const mount of extraMounts) {
+      mountSet.add(mount);
     }
-    addMount(`${hostClaudeProjects}:/root/.claude/projects`);
-  } else {
-    addMount(`${path.join(projectDir, "claude-projects")}:/root/.claude/projects`);
-  }
-
-  // Feature flag cache — always per-container
-  addMount(`${path.join(projectDir, "claude-statsig")}:/root/.claude/statsig`);
-
-  // Git config read-only
-  const gitconfig = path.join(home, ".gitconfig");
-  if (fs.existsSync(gitconfig)) {
-    addMount(`${gitconfig}:/root/.gitconfig:ro`);
   }
 
-  return mounts;
+  return Array.from(mountSet);
 }
